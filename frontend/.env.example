# Leave empty for local dev — Vite proxies /api and /webhook to localhost:3000 (see vite.config.js).
# In production (frontend deployed separately from backend), set this to your deployed
# backend's origin, e.g. https://nirvha-backend.onrender.com (no trailing slash).
VITE_API_URL=
